interface:
  display_name: "gbird"
  short_description: "Generate verified coding-agent failure reports"
  default_prompt: "Use $gbird to analyze this repository’s coding-agent sessions, replay each candidate failure blindly, and generate its verified report."
